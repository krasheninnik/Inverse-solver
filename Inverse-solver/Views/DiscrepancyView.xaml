<Window x:Class="Inverse_solver.Views.DiscrepancyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Inverse_solver.Views"
        mc:Ignorable="d"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="DiscrepancyView" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*"></ColumnDefinition>
                <ColumnDefinition Width="20*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Grid.Row="0">
                <oxy:PlotView Model="{Binding DiscrepancyModel}"></oxy:PlotView>
            </Grid>
            <StackPanel Grid.Column="1" Grid.Row="0" Margin="15">
                <Button Margin="0,40,0,0" Content = "Change THIS" Command="{Binding OpenSettingsFormCommand}"></Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
